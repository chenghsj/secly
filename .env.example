# No auth environment variables are required for the current gh-first flow.
#
# Secly now reuses the local GitHub CLI session from `gh auth status`
# and starts login via `gh auth login --web`.
#
# Keep this file as a placeholder for future optional configuration.